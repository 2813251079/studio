// This flag is used to enable or disable Firebase functionality.
// To use Firebase authentication, you would need to add your credentials
// and re-enable the full Firebase initialization logic.
export const firebaseEnabled = false;

// Mock auth object for compatibility with components that might expect it.
// In a real Firebase setup, this would be the actual Auth instance.
export const auth = null;
